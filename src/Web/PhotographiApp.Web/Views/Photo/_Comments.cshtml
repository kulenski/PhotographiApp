@using PhotographiApp.Web.ViewModels.Comments;
@model IEnumerable<CommentViewModel>

@if (@Model.Count() == 0)
{
    <span>There are not comments!</span>
}
else
{
    foreach(var comment in Model)
    {
        <h5>(@comment.UserId) @comment.Value</h5>
        <hr/>
    }
}
